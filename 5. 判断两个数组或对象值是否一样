## 引用类型值是否一样
有时候我们需要判断两个数组保存的值是否一样, 如: [1,2] 和 [1,2]<br>
但是在JS中, 引用类型(如: 对象, 数组, 函数)保存的都是地址, 所以不能用 == 去判断两个引用类型保存的值是否一样, 需要用一些其他方法判断

### JSON.stringify
JSON.stringify([1,2]) === JSON.stringify([1,2])  // true

### toString 不推荐
因为[1,'2'].toString()  // "1,2"
